{
  "type": "object",
  "properties": {
    "cmd": {
      "oneOf": [
        { 
          "type": "array"
          "items": {
            "type": "string"
          },
          "minItems": 1
        },
        {
          "type": "string"
        }
      ]
    }
    "on_complete": { "type": ["string", "number"] },
    "on_fail": { "type": ["string", "number"] }
  },
  "required": ["cmd", "on_complete", "on_fail"]
}
