import React from "react";
import Modal from "react-bootstrap/Modal";

type CreateFilterModalProps = {
  show: boolean;
  onClose: () => void;
};

const CreateFilterModal = ({ show, onClose }: CreateFilterModalProps) => {
  return <Modal></Modal>;
};
export default CreateFilterModal;
