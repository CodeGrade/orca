{
  "name": "orca-web-server",
  "version": "1.0.0",
  "description": "Web server for the grading platform Orca; sister to Bottlenose.",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "start": "node build/index.js",
    "dev": "tsc --build && node build/index"
  },
  "author": "Jackson Williams",
  "license": "MIT",
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "@types/node": "^16.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "nodemon": "^2.0.19",
    "redis": "^4.2.0"
  }
}
