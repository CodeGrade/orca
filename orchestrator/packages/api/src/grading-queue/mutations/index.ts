import createOrUpdateJob from "./create-or-update-job";
import deleteJob from "./delete-job";
import enqueueImageBuild from "./enqueue-image-build";
import moveJob from "./move-job";

export default {
  createOrUpdateJob,
  enqueueImageBuild,
  moveJob,
  deleteJob,
};
