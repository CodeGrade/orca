{
  "name": "@codegrade-orca/api",
  "version": "1.0.0",
  "description": "Web server for the grading platform Orca; sister to Bottlenose.",
  "main": "dist/",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "check": "tsc --noEmit",
    "dev": "nodemon --exec ts-node --require tsconfig-paths/register src/index.ts",
    "start": "node dist/index.js",
    "test": "jest"
  },
  "author": "Jackson Williams",
  "license": "MIT",
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.13"
  },
  "dependencies": {
    "@codegrade-orca/common": "^1.0.0",
    "@codegrade-orca/db": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.1"
  }
}
